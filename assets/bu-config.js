/* ═══════════════════════════════════════════════════════════════════════════
   本部门配置（中性占位版，随仓库公开）

   这里必须匹配真实 Excel 的几个值不写死在代码里，集中到本文件。
   仓库里的这份是**占位值**；你自己的真实值写在同目录的 bu-config.local.js，
   它在仓库里被忽略（.gitignore），加载顺序在本文件之后，会逐键覆盖。

   ⚠ 两个文件都要能被浏览器与 node 工具读到：
     - 浏览器：看板.html 里按 bu-config.js → bu-config.local.js 的顺序 <script> 引入
     - node  ：工具/生成数据包.js 等通过 工具/读取部门配置.js 读同一组文件
   ⚠ 键名不要改，改了代码取不到值；缺键时各取用处有自己的兜底。
   ═══════════════════════════════════════════════════════════════════════════ */
window.BU = {

  /* ── 本部门的标识 ──────────────────────────────────────────────────────
     code  中心代号：数据「一级部门英文简称」列里代表本部门的值。
           也是分享版裁剪、筛选器默认锁定、切片自检的比对值。
     label 界面与文档里显示用的称呼。 */
  code:  'BU',
  label: '本部门',

  /* ── 归属判定 ──────────────────────────────────────────────────────────
     orgMap 组织名关键字 → 中心代号的兜底映射，按顺序首个命中者生效。
            仅在「一级部门英文简称」列为空时才用得上；留空数组 = 不做这层兜底。 */
  orgMap: [],

  /* ── 表名 / 文件名 ─────────────────────────────────────────────────────
     rosterSheet 花名册 sheet 名。必须锚定开头，否则「本年度入职名单」
                 「高潜名单」「C型干部名单」都会被含「名单」的宽松匹配命中。
     book        data/ 下你手工维护的那张汇总表。 */
  rosterSheet: /^人员名单/,
  book:        '人力看板.xlsx',

  /* ── data/ 下的兜底文件名（file:// 双击打开时走数据包，通常用不到） ──── */
  files: {
    cons:   'data/HR看板数据源.xlsx',
    emp:    'data/KPA.xls',
    perf:   'data/绩效考评结果汇总.xlsx',
    ledger: 'data/人才现状.xlsx',
  },

  /* 分享版数据包里 scope 字段用的标识（跟 code 走，一般不用改） */
  get scope() { return String(this.code || 'bu').toLowerCase(); },
};

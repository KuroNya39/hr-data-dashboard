# HR 数据看板 · 使用指南

> **双击 `看板.html` 就能用。** 不需要启动任何程序、不用装 Python、不用命令行、不联网也能看。

---

## 一、打开看板

在项目文件夹里双击 **`看板.html`**。

- 首次打开约 **1 秒**（解析 1865 人 + 绩效 + 台账）
- 之后再打开约 **0.5 秒**（走浏览器本地缓存）

侧边栏左上角写着「HRBP 工作台」，标题栏页面依次是：总览 / 组织与用工 / 人员结构 / 人员流动 / 转正与合同 / 梯队绩效盘点 / 职级健康度 / HR 工作台账 / 员工明细 / 简报模式。

**注意**：不要再去找 `.bat` 文件，那些已经删掉了。现在只需要双击 HTML。

---

## 二、更新数据

改完 Excel 后，有三种更新方式，选一种就行。

### 方式 ①　从 data 文件夹读取　（最省事，推荐日常用）

1. 点顶栏 **「数据维护」**
2. 点最上面那条 **「从 data 文件夹读取最新数据」**
3. 第一次会弹出文件夹选择框 —— 选中本项目里的 **`data`** 文件夹，允许访问
4. 以后就只要点按钮，**不用再选文件夹**（浏览器记住了）

> 电脑重启、或换了浏览器之后，可能需要再授权一次，点一下允许即可。

### 方式 ②　把 Excel 拖进页面　（临时用）

直接把 Excel 文件拖到看板页面上，或者点顶栏「上传人员数据 / 上传绩效数据 / 上传台账」。

数据会立刻更新，并且**自动记住**，下次打开还是它。
但这种方式**不会改动数据文件**，适合「我就想临时看一眼新导出的表」。

### 方式 ③　保存为数据包　（让改动长期生效）

点「数据维护」→ **「把当前数据保存为数据包」** → 选中本项目根目录。

它会把当前看板上的数据写进 `assets/hr-data.js`。这样即使换一台电脑、或者清空了浏览器缓存，打开看到的还是这份数据。

> 这三种方式的关系：
> - 方式 ①② 改的是「**你现在看到的内容**」（存在浏览器里）
> - 方式 ③ 改的是「**数据文件本身**」
>
> 如果想让自己的改动传给同事，用方式 ③ 之后再走下面的分享流程最稳。

---

## 三、分享给 SWC 同事

### 一次性准备（已经做好了，不用重复）

项目里有一个 **`分享版`** 文件夹，里面是一份**独立、完整、只有 SWC 数据**的看板。
它不依赖任何外部文件，同事解压后**双击里面的 `看板.html` 就能看**，不用装任何东西。

### 每次分享前：更新分享版的数据

**方式 A（推荐）：用花名册裁**
1. 先按「二」把完整版数据更新到最新
2. 点「数据维护」→ **「用花名册导出分享版」**
3. 选一下 **`分享版`** 文件夹
4. 看到「保留 867 人 · 剔除 998 行」的提示就成功了

它按汇总表里的 **SWC花名册** 说话 —— **你想让谁进来/不进来，改那张表删行就行**，不用碰别的。

**方式 B：按中心自动裁**
点「数据维护」→ **「导出分享版数据（仅 SWC）」**。不看花名册，直接按「一级部门英文简称 = SWC」筛。

**方式 C：临时用一份外部名单**
点「数据维护」→ **「用指定名单导出分享版」**，选一份名单 Excel（要有「员工编号」列）。
适合「这次只想发某几个部门」。这种方式**不会影响**完整版当前的数据。

> 三种方式都是**物理裁剪** —— 产物文件里根本不存在名单外的人。
> 导出时如果自检发现越界行，会直接中止并提示，不会悄悄发出去。

### 发给同事

把整个 **`分享版`** 文件夹**压缩成 zip** 发出去。同事解压到任意位置，双击 `看板.html` 即可。

分享版的特点：
- 只有 SWC 一个中心的数据，**文件里根本没有其他中心**（不是靠界面隐藏）
- 顶栏没有上传和数据维护按钮，只有只读的浏览
- 顶部有一条「本看板仅包含 SWC 中心数据」的横幅
- 自带字体和图表库，**断网也能用**

---

## 四、数据文件放在哪

### data/ 里只有两份 Excel，各管一件事

| 文件 | 谁维护 | 用途 |
|---|---|---|
| **`SWC人力看板.xlsx`** | **你手动维护** | HTML 看板的原数据表：绩效、台账各名单、SWC人员名单都在里面 |
| **`KPA*.xls`** | 你从系统导出更新 | **总体人员花名册基准**。看板的人员数据一律以这份原文件为准 |

### `SWC人力看板.xlsx` 里的表（现 12 张）

| 里面的 sheet | 怎么更新 |
|---|---|
| **SWC人员名单** | 你每次从 KPA **手动复制粘贴**覆盖。它同时是分享白名单——**删掉一行 = 以后分享版不再发这个人** |
| **工作分工** | 每个人的主要工作内容（已按名单预填 867 人的工号/姓名/部门，你只填「主要工作内容」列）。**键 = 工号**，KPA 更新名单不影响这里；员工明细表会自动按工号带出这一列。新入职的人把行补上即可 |
| 干部梯队26H1绩效考评明细 / 人才梯队26H1绩效考评明细 | 你维护的绩效明细 |
| 绩效B-C人员情况 / C型干部名单 / 储备干部名单 / 高潜名单 | 你的干部与梯队台账 |
| 27届校招需求 / 26届校招名单 / 外包评价 / 招聘未达成需求 | 你的招聘与外包台账 |

> 注：你已删掉「本年度入职名单」「部门梯队」两张——看板对应板块随之消失，想恢复把 sheet 加回来就行（代码还认）。

看板按 **sheet 名关键字**认表（不认位置），所以 sheet 名里的关键字别删（如「绩效考评明细」「人员名单」），
列顺序随便调、插空列都行——解析按表头名找列。

> ⚠️ 别删「招聘未达成需求」这张表的表头行之外乱插入百万空行——从系统复制内容时如果带了
> 超大范围，看板读数会变慢（看板已做防护，但保持文件干净更好）。

### 改完之后

1. 双击 `看板.html` → 数据维护 → **「从 data 文件夹读取最新数据」**（第一次授权选一下 data 文件夹）
2. KPA 换了新导出 → 把新文件放进 `data/`（旧的删掉），看板读人员自动取**修改时间最新**的那份
3. 数据改完想长期固化 → 「把当前数据保存为数据包」
4. 发 SWC 管理人员 → 「用花名册导出分享版」（按 **SWC人员名单** 裁）→ 压缩 `分享版/` 发出去

### 编制 / 目标类指标的说明

旧架构里的「部门编制 / 指标目标 / 月度快照 / 招聘计划」四张手工表已按你的要求移除，
看板上编制达成率、目标值这类指标会显示「—」——不是坏了。哪天要恢复，把对应 sheet
加回 `SWC人力看板.xlsx`（sheet 名用回这四个词）即可，代码还认。

---

## 五、目录说明

```
HR看板/
├─ 看板.html                 ← ★ 双击这个（完整版）
├─ 分享版/                   ← 打包发给同事的（双击里面的 看板.html）
│   ├─ 看板.html
│   └─ assets/
├─ assets/                   ← 代码与数据包（不用动）
│   ├─ css/  js/  vendor/
│   └─ hr-data.js            ← 数据包（由「保存为数据包」生成）
├─ data/                     ← ★ 只有你的两份 Excel
│   ├─ SWC人力看板.xlsx       ← ★ 你手动维护的原数据表
│   └─ KPA*.xls              ← 人员花名册基准（系统导出）
├─ 工具/                     ← 维护脚本（平时用不到，见第七节）
├─ 使用指南.md
└─ CLAUDE.md
```

---

## 六、常见问题

**Q：双击 `看板.html` 一片空白 / 没有数据？**
A：确认是在项目的这个文件夹里打开的、且 `assets/hr-data.js` 还在。
   如果数据包丢了，点「数据维护」→「从 data 文件夹读取最新数据」重新读一遍即可。

**Q：`data` 文件夹里的 Excel 换了，看板怎么没变？**
A：看板优先显示浏览器缓存里「你上次看到的数据」。点「数据维护」→「从 data 文件夹读取最新数据」。

**Q：想让看板回到出厂状态（回到数据包里的数据）？**
A：点「数据维护」→「回到数据包」。

**Q：换电脑 / 换了浏览器，数据还在吗？**
A：浏览器缓存不跟着走，但 `assets/hr-data.js` 在。所以打开看到的是数据包里的数据，
   想同步最新改动，先点「保存为数据包」再拷贝整个文件夹。

**Q：同事打开分享版是白屏？**
A：确认是解压之后双击**文件夹里**的 `看板.html`，而不是在压缩包里直接点。
   分享版是自包含的，不需要联网、不需要装任何东西。

**Q：浏览器提示「不支持直接读写文件」？**
A：用 **Chrome** 或 **Edge** 打开即可。拖 Excel 进页面的方式不受影响。

---

## 七、给维护者

数据换了新的原始导出，**按这个顺序跑**（两步，别跳）：

```
node 工具/生成数据包.js       # ① KPA + SWC人力看板.xlsx → assets/hr-data.js（完整版）
node 工具/生成分享版.js       # ② 出分享版 + 跑安全自检（内部会调 生成数据包.js --swc）
```

（旧的三步链路里的 `建汇总表.js` 已删除——现在 SWC人力看板.xlsx 是你手工维护的，不再自动搬运。）

日常不改数据、只改 `assets/js/` 或 `assets/css/` 代码时，**只需要跑 ②**，否则 `分享版/assets/` 里还是旧副本。

② 的自检会验：分享版人员每行工号必须在「SWC人员名单」内、分享版目录不得含 KPA 原件、
HTML/JS 里不得出现其他中心名、不得有 bat/cmd/vbs 脚本。

### 必须知道的坑

**① 别用 `fs.rmSync(dir,{recursive:true})` / `fs.cpSync`**
vendor 下有 100+ 个字体分片，原生递归会栈溢出崩掉（exit 0xC0000409）。
两个脚本里都已经换成显式栈式遍历，改动时别退回原生调用。

**② 手工重写 xlsx 的 zip 结构时，EOCD「中央目录偏移」必须填「中央目录的起点」**
（历史教训，已删的 `建汇总表.js` 在这里翻过车）：填成 body 长度会指向 EOCD 自己，
**Excel 打不开（报文件已损坏）**，而 SheetJS 容错读得出来，问题会被掩盖。
写了 zip 重打包逻辑，务必用 .NET `Expand-Archive` 之类的严格解析器交叉验证。
